{
  "version": "0.2.0",
    "configurations": [

        {
            "name": "Python: Current File",
            "type": "python",
            "request": "launch",
            "program": "${workspaceRoot}/Crawler/main.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceRoot}/Crawler",
            "env": {
                "PYTHONPATH": "${workspaceRoot}"
            },
            "args": [
                "Phone"
            ]
        }
    ]
}